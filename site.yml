---
 - hosts: localhost
   roles:
    - ansible-pacman
    - ansible-arch-xorg
    - ansible-ssh
    - ansible-ssh-server
    - ansible-ufw
    - ansible-git
    - ansible-docker
    - ansible-arch-bluetooth-keyboard
    - ansible-hostname