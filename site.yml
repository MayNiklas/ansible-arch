---
 - hosts: localhost
   roles:
    - ansible-pacman
    - ansible-ssh
    - ansible-ssh-server
    - ansible-ufw
    - ansible-git
    - ansible-docker
    - ansible-arch-bluetooth-keyboard
    - ansible-hostname